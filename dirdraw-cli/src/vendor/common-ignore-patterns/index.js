/*
 * Vendored from: @alexstevovich/common-ignore-patterns
 * Copied locally; modify independently.
 */

export default Object.freeze([
  'node_modules/',
  'dist/',
  'build/',
  'coverage/',
  '.git/',
  '.cache/',
  '.DS_Store',
  'Thumbs.db',
])
